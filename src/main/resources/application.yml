spring:
  datasource:
    url : jdbc:h2:tcp://localhost/~/marketit
    username : sa
    password : 123
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
#        show_sql : true
        format_sql : true
    open-in-view: true
